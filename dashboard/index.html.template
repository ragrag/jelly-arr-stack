<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Media Server</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: "Inter", -apple-system, BlinkMacSystemFont, sans-serif;
            height: 100vh;
            overflow: hidden;
            color: #fff;
        }
        .container {
            display: flex;
            height: 100vh;
        }
        .sidebar {
            width: 400px;
            background: #0a0a0a;
            padding: 60px 40px;
            display: flex;
            flex-direction: column;
            gap: 20px;
            overflow-y: auto;
        }
        .bg-section {
            flex: 1;
            background:
                linear-gradient(to left, rgba(0,0,0,0.3) 0%, rgba(10,10,10,0.8) 100%),
                url("https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=1920&q=80") center/cover no-repeat;
        }
        .card {
            background: rgba(30, 30, 30, 0.8);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 12px;
            padding: 16px 24px;
            text-decoration: none;
            color: #fff;
            transition: all 0.2s ease;
            display: flex;
            align-items: center;
            gap: 16px;
        }
        .card:hover {
            background: rgba(229, 9, 20, 0.15);
            border-color: rgba(229, 9, 20, 0.4);
            transform: translateX(4px);
        }
        .card-icon {
            font-size: 28px;
            width: 40px;
            text-align: center;
        }
        .card-action {
            font-size: 16px;
            font-weight: 500;
        }
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
            }
            .sidebar {
                width: 100%;
                height: 100vh;
            }
            .bg-section {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="sidebar">
            <a href="https://jellyseerr.{{DOMAIN}}" class="card">
                <span class="card-icon">📺</span>
                <div class="card-action">Watch</div>
            </a>
            <a href="https://jellyfin.{{DOMAIN}}" class="card">
                <span class="card-icon">🎬</span>
                <div class="card-action">Jellyfin</div>
            </a>
            <a href="https://radarr.{{DOMAIN}}" class="card">
                <span class="card-icon">🎥</span>
                <div class="card-action">Add Movies</div>
            </a>
            <a href="https://sonarr.{{DOMAIN}}" class="card">
                <span class="card-icon">📺</span>
                <div class="card-action">Add Shows</div>
            </a>
            <a href="https://bazarr.{{DOMAIN}}" class="card">
                <span class="card-icon">💬</span>
                <div class="card-action">Subtitles</div>
            </a>
            <a href="https://prowlarr.{{DOMAIN}}" class="card">
                <span class="card-icon">🔎</span>
                <div class="card-action">Indexers</div>
            </a>
            <a href="https://qbit.{{DOMAIN}}" class="card">
                <span class="card-icon">⬇️</span>
                <div class="card-action">Downloads</div>
            </a>
        </div>
        <div class="bg-section"></div>
    </div>
</body>
</html>
